---
title: Reading List
---

%h1 Reading List

%p A log of my reading. Favorites are marked with a 👍.

- bookCount = data.reading.count { |book| book.author.present?}

%table.table{style:"counter-reset:rowNumber #{bookCount+1}"}
  - data.reading.each do |book|
    - if book.header.present?
      %tr.htr
        %th{colspan: "3"}= book.header
    - else
      %tr
        %td
        %td
          = book_link_to book
          = "👍" if book.favorite?
        %td= book.author
