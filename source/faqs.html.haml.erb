---
title: Skeleton faqs
---

%h1 Skeleton FAQs

- data.faqs.each do |f|
  %h4 #{f.category}
  %ul
    - f.faqs.each do |faq|
      %li
        = link_to faq.q, "##{faq.q.parameterize}"

%dl
  - data.faqs.each do |f|
    - f.faqs.each do |faq|
      = content_tag :dt, id: faq.q.parameterize do
        = faq.q
        = link_to "&#8593;", "#"
      %dd= faq.a
